#include <stdio.h>
#include <stdlib.h>

int main()
{
    char longestside
    printf("enter the side lengths of the triangle");
    scanf("%d%d%d",&a&b&c);
    if(a>b && a>c){
       longestside='a';
    }else if(b>a && b>c)
    {
    longest side='b';
    }else{
    longestside='c';
    }

    return 0;
}
